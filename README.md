# Program-With-Do-While-Loop
A do-while loop is a control structure that executes a block of code at least once before checking the condition. After execution, it evaluates the condition; if true, the loop repeats. It is useful when the task must run at least once, such as menu-driven programs or user input validation.
